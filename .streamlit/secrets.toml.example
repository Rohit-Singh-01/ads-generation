# Streamlit Secrets Configuration (Example)
# Copy this to secrets.toml and configure in Streamlit Cloud

[playwright]
# Set to true to force Playwright usage (fail if not available)
force_playwright = false

# Set to true to allow fallback to lightweight scraper
allow_fallback = true

# Headless mode (should be true on Streamlit Cloud)
headless = true
